# Copy this file to `.env` and fill in your own values.
# `.env` is gitignored and must never be committed.
#
# Only the RotoWire *subscription* downloader needs credentials. Every other
# scraper reads public pages and needs nothing here.

ROTOWIRE_USERNAME=your_username_here
ROTOWIRE_PASSWORD=your_password_here
